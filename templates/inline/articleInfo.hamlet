<h3>
  <a href=@{PermalinkR $ articleSlug article}>#{articleTitle article}
<p><i class="icon-calendar"></i>_{MsgPostedTime $ humanReadableTime' now $ articleCreatedAt article}
<p><i class="icon-user"></i>_{MsgPostedAuthor screenAuthor}
$if not (null tags)
  <div .tags>
    <i .icon-tags>
    <ul .tag-list>
      $forall tag <- tags
        <li>
          <a href=@{TagR tag}>
            #{tag}
<p>#{articleContent article}